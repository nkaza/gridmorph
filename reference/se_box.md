# Square structuring element

`radius` cells in every direction (so a `2*radius + 1` wide matrix of
all `1`s).

## Usage

``` r
se_box(radius)
```

## Arguments

- radius:

  integer \>= 0

## Value

a plain matrix, all `1`s
