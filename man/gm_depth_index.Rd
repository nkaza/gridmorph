% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth-index.R
\name{gm_depth_index}
\alias{gm_depth_index}
\title{Depth index for a terra raster}
\usage{
gm_depth_index(rast, weighted = TRUE, n_bins = 1000)
}
\arguments{
\item{rast}{a terra SpatRaster. The shape is derived directly from
\code{rast}: a cell is part of the shape iff its own value is neither \code{NA}
nor exactly \code{0} - both are holes, no separate mask argument.}

\item{weighted}{logical, default \code{TRUE}. When \code{TRUE}, \code{rast}'s own cell
values are used as the density/mass throughout. When \code{FALSE}, every
valid cell is treated as equally massed regardless of \code{rast}'s actual
values - exactly reproducing the plain (unweighted) index even if
\code{rast} is itself a continuous raster. If \code{rast} is a CATEGORICAL
raster (\code{terra::is.factor(rast)} is \code{TRUE} - e.g. a land-use
classification with an attached levels table) and \code{weighted = TRUE},
the category CODE NUMBERS themselves get used as mass, which is
rarely the intended comparison (a class coded \code{3} outweighs one coded
\code{1} for no meaningful reason) - a warning is issued but the
computation still proceeds on the literal codes, since \code{weighted = TRUE} was explicitly requested; pass \code{weighted = FALSE} for
categorical rasters unless the codes really are meant as magnitudes.
This check never fires on non-categorical (including plain binary
0/1) rasters, so \code{weighted = TRUE} is always safe there.}

\item{n_bins}{integer, the exact/binned threshold and bin count for the
weighted reference's concentric-rings construction (only matters when
\code{weighted = TRUE} and the density genuinely varies - \code{weighted = FALSE}
always collapses to a single ring regardless of \code{n_bins}) - exact (no
accuracy cost) when the number of valid cells is \verb{<= n_bins}, a K-bin
histogram approximation above that. Default \code{1000} measured at ~0.1\%
relative error on a genuinely non-uniform weight pattern; see
\code{.adaptive_density_bins()} in \code{R/utils.R}.}
}
\value{
list(index, mean_depth, ref_depth, area, n_valid_cells). Computing
\code{ref_depth} costs one more \code{terra::distance()} call, on a freshly
rasterized disk of the same cell size and area as \code{rast} (not on
\code{rast} itself) - see file header for why a closed-form reference isn't
used here the way every other index in this package uses one.
}
\description{
Mean distance from inside points to the shape's own boundary, relative
to an equal-area circle. index in \verb{(0, 1]}, \verb{= 1} iff the shape is
(almost everywhere) a disk - see the file header for the
Brunn-Minkowski-based proof this ports from
\code{shapeindices::depth_index()}. Handles holes and multi-part shapes with
no special-casing at all: they're just cells with different values in
one grid.
}
\examples{
r <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0, ymax = 40, crs = "local")
terra::values(r) <- 0
r[10:30, 10:30] <- 1
gm_depth_index(r)$index
}
