% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classical-metrics.R
\name{gm_detour_index}
\alias{gm_detour_index}
\title{Detour compactness score of a terra raster}
\usage{
gm_detour_index(rast)
}
\arguments{
\item{rast}{a terra SpatRaster. The shape is derived directly from
\code{rast}: a cell is part of the shape iff its own value is neither \code{NA}
nor exactly \code{0} - both are holes, no separate mask argument.}
}
\value{
\code{list(index, area, hull_perimeter, ref_index, hull, n_valid_cells)}.
\code{hull} is the convex hull itself, a terra SpatVector, for plotting.
\code{ref_index} is the reference disk's own raw score - \code{index} itself is
\code{(2*sqrt(pi*area)/hull_perimeter) / ref_index}, not the raw ratio
directly.
}
\description{
\code{raw / ref_index}, in \verb{(0, 1]}, \code{1} = a circle, where \code{raw} is the
ratio of the equal-area circle's ANALYTICAL perimeter to the perimeter
of the shape's own convex hull, and \code{ref_index} is that SAME formula's
own score on an actual rasterized disk at \code{rast}'s own cell size and
area. Angel, Parent & Civco (2010) introduce this to measure how hard a
shape is to circumnavigate as an obstacle. The hull's own perimeter
needs no marching-squares correction (unlike
\code{gm_polsby_popper_index()}'s raw shape perimeter) - \code{terra::convHull()}'s
output is already a proper straight-edged polygon, not a raster-traced
boundary.
}
\details{
WHY NOT JUST \code{raw}: under centre-based rasterization, a boundary cell's
own far CORNER can sit outside the shape's true continuous boundary by
up to half a cell's diagonal, so the hull built from those corners has
vertices that genuinely protrude past the true circle - its perimeter
comes out LARGER than the analytical circle's own, even for an actual
disk. That scored \code{raw = 0.982} at a representative resolution.
Dividing by \code{ref_index} instead cancels it, since both numerator and
reference go through the identical hull-perimeter pipeline: the same
disk scores \code{0.997-1.000} this way, across resolutions (verified: low
phase sensitivity here, unlike \code{gm_polsby_popper_index()}'s own
marching-squares perimeter - see this file's own header). No
\code{analytical_ref} opt-out is provided: the raw ratio is not a more
"correct" number to fall back to, it is measurably the wrong one for a
rasterized shape at any finite resolution.
No \code{weighted} argument - see this file's own header for why.
}
\examples{
r <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0, ymax = 40, crs = "local")
terra::values(r) <- 0
r[10:30, 10:30] <- 1
gm_detour_index(r)$index
}
