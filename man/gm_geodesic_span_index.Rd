% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geodesic-index.R
\name{gm_geodesic_span_index}
\alias{gm_geodesic_span_index}
\title{Geodesic span index for a terra raster}
\usage{
gm_geodesic_span_index(
  rast,
  weighted = TRUE,
  n_points = 40,
  seed = NULL,
  n_bins = 100
)
}
\arguments{
\item{rast}{a terra SpatRaster. The shape is derived directly from
\code{rast}: a cell is part of the shape iff its own value is neither \code{NA}
nor exactly \code{0} - both are holes, no separate mask argument.}

\item{weighted}{logical, default \code{TRUE}. When \code{TRUE}, \code{rast}'s own cell
values are used as the density/mass throughout. When \code{FALSE}, every
valid cell is treated as equally massed regardless of \code{rast}'s actual
values - exactly reproducing the plain (unweighted) index even if
\code{rast} is itself a continuous raster. If \code{rast} is a CATEGORICAL
raster (\code{terra::is.factor(rast)} is \code{TRUE} - e.g. a land-use
classification with an attached levels table) and \code{weighted = TRUE},
the category CODE NUMBERS themselves get used as mass, which is
rarely the intended comparison (a class coded \code{3} outweighs one coded
\code{1} for no meaningful reason) - a warning is issued but the
computation still proceeds on the literal codes, since \code{weighted = TRUE} was explicitly requested; pass \code{weighted = FALSE} for
categorical rasters unless the codes really are meant as magnitudes.
This check never fires on non-categorical (including plain binary
0/1) rasters, so \code{weighted = TRUE} is always safe there.}

\item{n_points}{number of points to sample (ALL pairs among them are
used - see above, this is NOT \code{gm_span_index()}'s own \code{size}
convention, deliberately a different argument name so the two never
collide through \code{gm_shape_indices()}'s own shared \code{...}). Checked
against a memory/time-derived ceiling before running.}

\item{seed}{optional RNG seed}

\item{n_bins}{integer, the exact/binned threshold and bin count for the
weighted reference's concentric-rings construction - see
\code{gm_span_index()}'s own doc for the same parameter's meaning}
}
\value{
\code{list(index, D, D_ref, area, n_valid_cells)}
}
\description{
Mean GEODESIC distance between two random interior points, relative to
an equal-area circle - the raster analogue of a shape-confined-path
variant of \code{shapeindices::span_index()} (see file header; deferred in
the vector package for lacking cheap shortest-path machinery, not
needed here). \code{index = D_ref/D}, in \verb{(0, 1]}, \verb{= 1} iff the shape is
(almost everywhere) a disk. Distinct from \code{gm_span_index()} (same
construction, Euclidean straight-line distance instead) - the two agree
exactly on any CONVEX shape (where the straight line between two
interior points never leaves it) and diverge only once concavity forces
a detour.
}
\details{
\code{n_points} means something DIFFERENT here than \code{size} does in
\code{gm_span_index()} - a DELIBERATELY DIFFERENT ARGUMENT NAME, not an
oversight, precisely to avoid this - see file header. \code{n_points = K}
draws K points and averages ALL K(K-1)/2 pairwise distances among them
(not K/2 independent pairs), so a much smaller \code{n_points} already
gives a comparable number of pairs - but each point costs one
whole-raster \code{terra::gridDist()} call, a substantially higher
per-point cost than \code{gm_span_index()}'s own closed-form Euclidean
distance. Checked against a memory/time-derived ceiling before running
(\code{formula = "geodesic"}, R/utils.R); hard-stops, not a silent clamp, if
exceeded.

BOTH \code{D} and \code{D_ref} carry real Monte Carlo noise - more than
\code{gm_span_index()}'s own estimate at a comparable sample size, since
\code{terra::gridDist()}'s own angular quantization adds variability on top
of ordinary point-sampling noise (verified: several percent spread
across seeds even at \code{n_points = 200} on a test disk). Increase
\code{n_points} for a more precise answer; there is no way to eliminate
this noise entirely the way the closed-form Euclidean reference has
none.
}
\examples{
r <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0, ymax = 40, crs = "local")
terra::values(r) <- 0
r[10:30, 10:30] <- 1
gm_geodesic_span_index(r, n_points = 25, seed = 1)$index
}
