% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morphology.R
\name{bottomhat}
\alias{bottomhat}
\title{Bottom-hat (black-hat) transform: highlights small dark gaps/notches
narrower than the structuring element. For binary/continuous input,
the real-valued residual \code{closing(mask) - mask} (reduces exactly to the
classic \verb{closing(mask) AND NOT mask} when input is 0/1). For
categorical input, a boolean indicator instead - \code{1} at cells that were
unlabelled (\code{NA}) but got filled by \code{closing()} - since subtracting
category codes is meaningless (see this file's own header for why).}
\usage{
bottomhat(mask, kernel = se_box(1))
}
\arguments{
\item{mask}{binary, continuous, or categorical SpatRaster}

\item{kernel}{a structuring element: any matrix, or one of \code{se_box()},
\code{se_disc()}, \code{se_diamond()}. \code{1} includes a cell in the footprint,
\code{NA} excludes it (matching \code{terra::focal()}'s own \code{w} convention) -
any other value errors (see \code{.check_kernel()}'s own comments for why).}
}
\value{
SpatRaster, same grid as \code{mask} (see \code{tophat()}'s own note on
value type and non-negativity; \code{NA} region described above, not the
same one)
}
\description{
Binary/continuous input specifically: \code{NA} only where \code{mask} was
ITSELF \code{NA} (genuinely missing data, not just background) AND beyond
one \code{kernel} radius of \code{mask}'s own footprint - narrower than
\code{tophat()}'s equivalent restriction, deliberately, on both counts.
Not "wherever \code{mask} is \code{NA}" the way \code{tophat()} is: \code{bottomhat()}'s
entire purpose is flagging small gaps ADJACENT to (not inside) the
shape, and a gap IS an \code{NA} cell in \code{mask} - restoring \code{NA} there would
erase exactly the cells this function exists to highlight. And not
"wherever \code{mask} is confirmed \code{0}" either: a plain binary (\code{0}/\code{1}, no
\code{NA} at all) mask has no genuinely missing data anywhere, so its
\code{bottomhat()} output stays \code{0}/\code{1} everywhere, matching the classic
definition exactly. The one-kernel-radius reach bound is exact, not a
heuristic margin: \code{closing()} can only ever differ from background
within one kernel radius of \code{mask}'s own footprint (dilate grows by at
most one radius, the subsequent erode can only give at most that much
back).
}
