% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classical-metrics.R
\name{gm_hull_ratio_index}
\alias{gm_hull_ratio_index}
\title{Convex hull area ratio of a terra raster}
\usage{
gm_hull_ratio_index(rast)
}
\arguments{
\item{rast}{a terra SpatRaster. The shape is derived directly from
\code{rast}: a cell is part of the shape iff its own value is neither \code{NA}
nor exactly \code{0} - both are holes, no separate mask argument.}
}
\value{
\code{list(index, area, hull_area, hull, n_valid_cells)}. \code{hull} is
the convex hull itself, a terra SpatVector, for plotting.
}
\description{
area / area(convex hull), in \verb{(0, 1]}, \code{1} = the shape is convex (equals
its own convex hull). No \code{weighted} argument - see this file's own
header for why area/hull are structurally unweighted quantities.
}
\examples{
r <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0, ymax = 40, crs = "local")
terra::values(r) <- 0
r[10:30, 10:30] <- 1
gm_hull_ratio_index(r)$index
}
