% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/morphology.R
\name{tophat}
\alias{tophat}
\title{Top-hat transform: highlights small bright features/protrusions
narrower than the structuring element. For binary/continuous input,
the real-valued residual \code{mask - opening(mask)} (reduces exactly to the
classic \verb{mask AND NOT opening(mask)} when input is 0/1). For
categorical input, a boolean indicator instead - \code{1} at cells that had
a label but lost it under \code{opening()} - since subtracting category
codes is meaningless (see this file's own header for why).}
\usage{
tophat(mask, kernel = se_box(1))
}
\arguments{
\item{mask}{binary, continuous, or categorical SpatRaster}

\item{kernel}{a structuring element: any matrix, or one of \code{se_box()},
\code{se_disc()}, \code{se_diamond()}. \code{1} includes a cell in the footprint,
\code{NA} excludes it (matching \code{terra::focal()}'s own \code{w} convention) -
any other value errors (see \code{.check_kernel()}'s own comments for why).}
}
\value{
SpatRaster, same grid as \code{mask}. Binary/continuous input: \code{NA}
outside \code{mask}'s own footprint, non-negative and real-valued inside
it wherever \code{kernel} includes its own centre (true for
\code{se_box()}/\code{se_disc()}/\code{se_diamond()} at every radius - a custom
kernel that excludes its own centre can violate this, a property of
grayscale morphology in general). Categorical input: plain \code{0}/\code{1},
not itself categorical - the result is a flag, not a category.
}
\description{
Binary/continuous input specifically: \code{NA} outside \code{mask}'s own
footprint, not a numeric \code{0} - \code{opening()}'s own output never actually
contains \code{NA} for non-categorical input (\code{.mask01()} replaces \code{NA}
with \code{0} internally so \code{focal()} treats it as background), so without
this restriction the residual would be a fully-defined \code{0} arbitrarily
far outside the shape. Restoring \code{NA} there loses no information:
\code{mask - opening(mask)} is exactly \code{0} everywhere outside \code{mask}'s own
footprint regardless (\code{opening(x) <= x} pointwise, and opening never
creates value beyond \code{mask}'s own extent).
}
