% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classical-metrics.R
\name{gm_polsby_popper_index}
\alias{gm_polsby_popper_index}
\title{Polsby-Popper compactness score of a terra raster}
\usage{
gm_polsby_popper_index(rast)
}
\arguments{
\item{rast}{a terra SpatRaster. The shape is derived directly from
\code{rast}: a cell is part of the shape iff its own value is neither \code{NA}
nor exactly \code{0} - both are holes, no separate mask argument.}
}
\value{
\code{list(index, area, perimeter, ref_index, n_valid_cells)}.
\code{ref_index} is the reference disk's own raw score - \code{index} itself is
\code{(4*pi*area/perimeter^2) / ref_index}, not the raw formula directly.
}
\description{
\code{raw / ref_index}, in \verb{(0, 1]}, \code{1} = a circle, where \code{raw} is the
textbook \code{4*pi*area/perimeter^2} (perimeter via marching squares,
\code{terra::as.contour()} - see this file's own header for why, verified
empirically against the naive cell-edge alternative) and \code{ref_index} is
that SAME formula's own score on an actual rasterized disk at \code{rast}'s
own cell size and area.
}
\details{
WHY NOT JUST \code{raw}: comparing it against the textbook constant \code{1}
(a claim about a perfect, infinitely-smooth circle) systematically
understates how compact a rasterized shape is. A rasterized DISK - the
one shape this score should never meaningfully penalize - scored \code{0.88}
on \code{raw} alone at a representative resolution, not because the shape is
imperfect, but because marching-squares perimeter overestimates a
raster boundary's true length, and squaring it in the denominator
roughly doubles that percentage error. Dividing by \code{ref_index} instead
cancels that measurement bias, since both numerator and reference go
through the identical perimeter-measurement pipeline: the same disk
scores \code{0.97-1.00} this way, across resolutions. This reference is
PHASE-AVERAGED (25 sub-cell offsets) rather than a single realization -
see this file's own header for why marching-squares perimeter needs
that where the other resolution-matched references in this file don't.
No \code{analytical_ref} opt-out is provided: the raw closed-form score is
not a more "correct" number to fall back to, it is measurably the wrong
one for a rasterized shape at any finite resolution.
No \code{weighted} argument - see this file's own header for why.
}
\examples{
r <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0, ymax = 40, crs = "local")
terra::values(r) <- 0
r[10:30, 10:30] <- 1
gm_polsby_popper_index(r)$index
}
