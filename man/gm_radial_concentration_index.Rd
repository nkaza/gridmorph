% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radial-concentration-index.R
\name{gm_radial_concentration_index}
\alias{gm_radial_concentration_index}
\title{Radial concentration index for a terra raster - the raster analogue of
\code{shapeindices::radial_concentration_index()}. \code{D1_ref/D1}, in
\verb{(0, 1]}, where \code{D1} is the mean distance from random interior points
to the shape's own geometric median (the point minimising that mean
distance - not the centroid) and \code{D1_ref} is the same quantity for the
reference shape (a circle, unweighted; a concentric annulus, weighted) -
both provable minimisers of \code{D1}.}
\usage{
gm_radial_concentration_index(
  rast,
  weighted = TRUE,
  size = 3000,
  seed = NULL,
  n_bins = 1000
)
}
\arguments{
\item{rast}{a terra SpatRaster. The shape is derived directly from
\code{rast}: a cell is part of the shape iff its own value is neither \code{NA}
nor exactly \code{0} - both are holes, no separate mask argument.}

\item{weighted}{logical, default \code{TRUE}. When \code{TRUE}, \code{rast}'s own cell
values are used as the density/mass throughout. When \code{FALSE}, every
valid cell is treated as equally massed regardless of \code{rast}'s actual
values - exactly reproducing the plain (unweighted) index even if
\code{rast} is itself a continuous raster. If \code{rast} is a CATEGORICAL
raster (\code{terra::is.factor(rast)} is \code{TRUE} - e.g. a land-use
classification with an attached levels table) and \code{weighted = TRUE},
the category CODE NUMBERS themselves get used as mass, which is
rarely the intended comparison (a class coded \code{3} outweighs one coded
\code{1} for no meaningful reason) - a warning is issued but the
computation still proceeds on the literal codes, since \code{weighted = TRUE} was explicitly requested; pass \code{weighted = FALSE} for
categorical rasters unless the codes really are meant as magnitudes.
This check never fires on non-categorical (including plain binary
0/1) rasters, so \code{weighted = TRUE} is always safe there.}

\item{size}{number of points to sample and run Weiszfeld's algorithm
on directly (no pairing, unlike \code{gm_convexity_index()}/\code{gm_span_index()}) -
matches \code{terra::spatSample()}'s own argument name and meaning
directly. Checked against a memory-derived ceiling before running;
hard-stops, not a silent clamp, if exceeded.}

\item{seed}{optional RNG seed}

\item{n_bins}{integer, the exact/binned threshold and bin count for the
weighted reference's concentric-rings construction. Default \code{1000},
same as \code{gm_depth_index()}'s - this reference is an exact closed-form
sum, not quadrature, so (unlike \code{gm_span_index()}) there's no O(rings^2)
cost to guard against.}
}
\value{
\code{list(index, D1, D1_ref, area, center, n_valid_cells)}. \code{center}
is the geometric median found by Weiszfeld's algorithm - may be
non-unique for a symmetric multi-part shape (see
\code{.geometric_median()}'s own comments), in which case it can land
anywhere along the minimising segment, including inside a hole or the
gap between multi-part pieces; the index value itself is unaffected.
}
\description{
Radial concentration index for a terra raster - the raster analogue of
\code{shapeindices::radial_concentration_index()}. \code{D1_ref/D1}, in
\verb{(0, 1]}, where \code{D1} is the mean distance from random interior points
to the shape's own geometric median (the point minimising that mean
distance - not the centroid) and \code{D1_ref} is the same quantity for the
reference shape (a circle, unweighted; a concentric annulus, weighted) -
both provable minimisers of \code{D1}.
}
\examples{
r <- terra::rast(nrows = 40, ncols = 40, xmin = 0, xmax = 40, ymin = 0, ymax = 40, crs = "local")
terra::values(r) <- 0
r[10:30, 10:30] <- 1
gm_radial_concentration_index(r, size = 2000, seed = 1)$index
}
